export const environment = {
  production: true,
  auth: {
    api: '', //bm-api-data host and port
    tokenStorageId: '', // token id in local storage
    authorityStorageId: '', // authority id in local storage
    profilenStorageId: '', // profile id in local storage
  },
};
